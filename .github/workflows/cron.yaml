name: order-meal-cron
on:
  schedule:
    - cron: '0 53 13,14 * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: order-meal
        run: |
          curl --request GET \
          --url 'https://order-meal-robot.vercel.app/order-meal/push'
