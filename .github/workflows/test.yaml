name: test
on:
  schedule:
    - cron: '*/10 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call Push Api
        run: |
          curl --request GET \
          --url 'https://order-meal-robot.vercel.app/order-meal/push'
